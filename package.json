{
  "name": "elderscape2",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r public/* dist/"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
